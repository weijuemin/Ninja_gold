# Ninja_gold
It's an easy mini game designed with Django 1.9 under virtual environment. Purpose is to practice form submission and processing.


# Django installation and set up:
Within an activated virtualenv folder (not the project folder), run following commands:
1. pip install django
2. django-admin startproject july15_ninja_gold
3. cd july15_ninja_gold

Then copy everything in this repo in.
